<p style="color: green"><%= notice %></p>

<%= render @tweet %>

<div>
  <%= link_to "新規作成", edit_tweet_path(@tweet) %> |
  <%= link_to "投稿一覧へ戻る", tweets_path %>

  <%= button_to "投稿を削除", @tweet, method: :delete %>
</div>
